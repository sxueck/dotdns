# Create the dotdns system user and group automatically via systemd-sysusers
# Usage: install to /usr/lib/sysusers.d/ or /etc/sysusers.d/ and run systemd-sysusers
u dotdns - "dotdns DNS resolver" /var/lib/dotdns
